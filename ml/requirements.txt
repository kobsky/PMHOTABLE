# U1 — task-type classifier (offline eval). Isolated Python project; NOT part of the Next.js app.
# CPU-only; torch CPU wheel is the pip default on Windows.
scikit-learn>=1.4
sentence-transformers>=2.7
pandas>=2.0
numpy>=1.26
scipy>=1.11
joblib>=1.3
datasets>=2.18
